import { DateDdmmyyyyPipe } from './date-ddmmyyyy.pipe';

describe('DateDdmmyyyyPipe', () => {
  it('create an instance', () => {
    const pipe = new DateDdmmyyyyPipe();
    expect(pipe).toBeTruthy();
  });
});
